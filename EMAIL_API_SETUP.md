# Email API Setup

Recommended provider: Brevo.

Add these in Render > Web Service > Environment:

EMAIL_PROVIDER=brevo
BREVO_API_KEY=your_brevo_api_key
EMAIL_FROM=Attendance System <your_verified_sender_email>

Then run Manual Deploy > Deploy latest commit.

See BREVO_SETUP.md for detailed steps.
